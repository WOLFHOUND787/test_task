# 🛍️ Маркетплейс

Система управления маркетплейсом с гибкой системой аутентификации, авторизации и ролевым доступом.

## 🚀 Особенности

- **Кастомная аутентификация**: Собственная система на JWT токенах
- **Ролевая модель**: Гибкая система ролей (user, manager, admin)
- **Гранулярные права**: Тонкая настройка прав доступа
- **Управление пользователями**: Полный контроль для администраторов
- **Система банов**: Временная и постоянная блокировка
- **Современный интерфейс**: Bootstrap 5 с адаптивным дизайном

## 🛠️ Технологический стек

- **Backend**: Django 4.2.7 + Django REST Framework 3.14.0
- **Frontend**: Bootstrap 5.1.3 + Vanilla JavaScript
- **База данных**: SQLite
- **Аутентификация**: JWT (PyJWT)
- **Хеширование**: bcrypt

## 📁 Структура проекта

```
test_task/
├── authentication/          # Система аутентификации
│   ├── models.py           # Модели пользователей, ролей, правил
│   ├── views.py            # API эндпоинты
│   ├── serializers.py      # Сериализаторы
│   ├── middleware.py       # Middleware аутентификации
│   └── urls.py            # URL маршруты
├── mock_business/          # Бизнес-логика
│   ├── models.py           # Модели магазинов, товаров, заказов
│   ├── views.py            # API эндпоинты
│   └── urls.py            # URL маршруты
├── templates/             # HTML шаблоны
│   └── marketplace.html    # Основной интерфейс
├── test_task/             # Настройки Django
├── requirements.txt        # Зависимости
├── API_DOCUMENTATION.md   # Документация API
└── DATABASE_SCHEMA.md     # Схема базы данных
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Создание и применение миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### 3. Создание суперпользователя

```bash
python manage.py createsuperuser
```

### 4. Запуск сервера

```bash
python manage.py runserver
```

### 5. Доступ к приложению

- **Основной интерфейс**: http://localhost:8000
- **API документация**: http://localhost:8000/api/
- **Админ панель**: http://localhost:8000/admin/

## 👥 Роли и права доступа

### User (Пользователь)
- Просмотр магазинов и товаров
- Создание заказов
- Управление профилем

### Manager (Менеджер)
- Все права пользователя
- Управление магазинами и товарами
- Просмотр заказов

### Admin (Администратор)
- Все права менеджера
- Полное управление пользователями
- Блокировка/разблокировка аккаунтов
- Удаление пользователей

## 🔐 API эндпоинты

### Аутентификация
- `POST /api/auth/register/` - Регистрация
- `POST /api/auth/login/` - Вход
- `POST /api/auth/logout/` - Выход
- `GET /api/auth/profile/` - Профиль пользователя

### Бизнес-логика
- `GET /api/business/shops/` - Список магазинов
- `POST /api/business/shops/` - Создание магазина
- `PUT /api/business/shops/{id}/` - Обновление магазина
- `DELETE /api/business/shops/{id}/` - Удаление магазина

### Управление пользователями (только для админа)
- `GET /api/business/users/` - Список пользователей
- `PUT /api/business/users/{id}/update/` - Обновление пользователя
- `DELETE /api/business/users/{id}/delete/` - Удаление пользователя

## 🎯 Особенности реализации

### Система аутентификации
- JWT токены с access/refresh парами
- Автоматическое обновление токенов
- Middleware для проверки аутентификации

### Система авторизации
- Гибкая система ролей и правил
- Проверка прав на уровне бизнес-элементов
- Разграничение доступа к своим/чужим объектам

### Управление пользователями
- Блокировка на определенный срок
- Массовые операции
- Защита от удаления суперпользователей

## 📝 Примеры использования

### Регистрация нового пользователя

```bash
curl -X POST http://localhost:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "first_name": "Иван",
    "last_name": "Иванов",
    "password": "password123",
    "password_confirm": "password123",
    "role": "user"
  }'
```

### Вход в систему

```bash
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123"
  }'
```

### Создание магазина (менеджер/админ)

```bash
curl -X POST http://localhost:8000/api/business/shops/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {token}" \
  -d '{
    "name": "Мой магазин",
    "address": "ул. Примерная, 123",
    "phone": "+7 (999) 123-45-67"
  }'
```

## 🔧 Конфигурация

### Настройки JWT
```python
JWT_SECRET_KEY = 'your-secret-key'
JWT_ALGORITHM = 'HS256'
JWT_ACCESS_TOKEN_LIFETIME = 60 * 60 * 24  # 24 часа
```

### CORS настройки
```python
CORS_ALLOW_ALL_ORIGINS = True
CORS_ALLOW_CREDENTIALS = True
```

## 🚨 Важные замечания

- Система разработана для демонстрации кастомной аутентификации
- Использует SQLite для простоты развертывания
- Frontend и backend работают вместе в одном приложении
- Все пароли хешируются с использованием bcrypt

## 📞 Поддержка

При возникновении проблем:
1. Проверьте правильность установки зависимостей
2. Убедитесь что миграции применены
3. Проверьте настройки JWT_SECRET_KEY
4. Очистите кэш браузера при проблемах с интерфейсом

---

**Разработано с использованием Django REST Framework и современных веб-технологий**
