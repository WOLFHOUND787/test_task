# API Документация - Маркетплейс

## Обзор

Документация описывает API маркетплейса с правильной логикой ролей и прав доступа.

## Базовый URL

```
http://localhost:8000/api
```

## Роли пользователей

### 🎯 **Administrator (Администратор)**
- **Роль:** Владелец всей площадки
- **Права:** Полный доступ ко всем ресурсам и операциям
- **Ответственность:** Управление системой, пользователями, всеми данными

### 🏪 **Manager (Менеджер)**  
- **Роль:** Управление магазином/товарами
- **Права:** 
  - ✅ Полное управление своими товарами (создание, редактирование, удаление)
  - ✅ Управление своим магазином
  - ✅ Просмотр заказов на свои товары
  - ✅ Управление своим профилем
  - ❌ Не может управлять магазинами и товарами других менеджеров
  - ❌ Не может управлять пользователями
- **Ответственность:** Контроль товарного ассортимента, обработка заказов

### 🛒 **User (Пользователь)**
- **Роль:** Клиент/покупатель
- **Права:**
  - ✅ Просмотр всех магазинов и товаров
  - ✅ Создание заказов на товары
  - ✅ Просмотр и управление своими заказами
  - ✅ Управление своим профилем
  - ❌ Не может управлять товарами, магазинами, пользователями
- **Ответственность:** Покупка товаров, отслеживание заказов

## Аутентификация

### 1. Регистрация

```bash
curl -X POST http://localhost:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "newuser@example.com",
    "full_name": "New User",
    "password": "password123",
    "password_confirm": "password123",
    "role": "user"
  }'
```

**Роли для регистрации:**
- `"user"` - Пользователь (по умолчанию)
- `"manager"` - Менеджер

### 2. Вход в систему

```bash
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"email": "user@example.com", "password": "user123"}'
```

**Ответ:**
```json
{
  "access_token": "eyJ...",
  "refresh_token": "eyJ...",
  "access_expires_at": "2026-02-04T11:25:04.240291Z",
  "refresh_expires_at": "2026-02-11T11:10:04.240303Z",
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "full_name": "User Name",
    "roles": [{"name": "user"}]
  }
}
```

### 3. Обновление токена

```bash
curl -X POST http://localhost:8000/api/auth/refresh/ \
  -H "Content-Type: application/json" \
  -d '{"refresh_token": "refresh_token_here"}'
```

### 4. Выход из системы

```bash
curl -X POST http://localhost:8000/api/auth/logout/ \
  -H "Authorization: Bearer access_token_here"
```

### 5. Получение профиля

```bash
curl -X GET http://localhost:8000/api/business/profile/ \
  -H "Authorization: Bearer access_token_here"
```

### 6. Обновление профиля

```bash
curl -X PUT http://localhost:8000/api/business/profile/ \
  -H "Authorization: Bearer access_token_here" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "newemail@example.com",
    "full_name": "Updated Name"
  }'
```

### 7. Удаление аккаунта

```bash
curl -X DELETE http://localhost:8000/api/business/profile/ \
  -H "Authorization: Bearer access_token_here"
```

## Бизнес-ресурсы

### 1. Магазины (/api/business/shops/)

#### Получение списка магазинов

```bash
# Администратор - видит все магазины
curl -X GET http://localhost:8000/api/business/shops/ \
  -H "Authorization: Bearer admin_access_token"

# Менеджер - видит только свой магазин
curl -X GET http://localhost:8000/api/business/shops/ \
  -H "Authorization: Bearer manager_access_token"

# Пользователь - видит все магазины для покупок
curl -X GET http://localhost:8000/api/business/shops/ \
  -H "Authorization: Bearer user_access_token"
```

**Результаты по ролям:**
- ✅ **Administrator**: Все магазины
- ✅ **Manager**: Только свой магазин
- ✅ **User**: Все магазины

#### Создание магазина (только для менеджеров)

```bash
curl -X POST http://localhost:8000/api/business/shops/create/ \
  -H "Authorization: Bearer manager_access_token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "My Shop",
    "address": "123 Main St",
    "phone": "+1-555-0123"
  }'
```

**Результаты по ролям:**
- ✅ **Administrator**: Доступ разрешен
- ✅ **Manager**: Доступ разрешен (создает свой магазин)
- ❌ **User**: Доступ запрещен

### 2. Продукты магазина (/api/business/shops/{shop_id}/products/)

#### Получение продуктов магазина

```bash
# Администратор - видит все продукты магазина
curl -X GET http://localhost:8000/api/business/shops/{shop_id}/products/ \
  -H "Authorization: Bearer admin_access_token"

# Менеджер - видит только продукты своего магазина
curl -X GET http://localhost:8000/api/business/shops/{shop_id}/products/ \
  -H "Authorization: Bearer manager_access_token"

# Пользователь - видит все продукты для покупок
curl -X GET http://localhost:8000/api/business/shops/{shop_id}/products/ \
  -H "Authorization: Bearer user_access_token"
```

**Результаты по ролям:**
- ✅ **Administrator**: Все продукты
- ✅ **Manager**: Только продукты своего магазина
- ✅ **User**: Все продукты

### 3. Заказы (/api/business/orders/)

#### Получение заказов

```bash
# Администратор - видит все заказы
curl -X GET http://localhost:8000/api/business/orders/ \
  -H "Authorization: Bearer admin_access_token"

# Менеджер - видит заказы на свои товары
curl -X GET http://localhost:8000/api/business/orders/ \
  -H "Authorization: Bearer manager_access_token"

# Пользователь - видит только свои заказы
curl -X GET http://localhost:8000/api/business/orders/ \
  -H "Authorization: Bearer user_access_token"
```

**Результаты по ролям:**
- ✅ **Administrator**: Все заказы
- ✅ **Manager**: Заказы на свои товары
- ✅ **User**: Только свои заказы

#### Создание заказа

```bash
curl -X POST http://localhost:8000/api/business/orders/create/ \
  -H "Authorization: Bearer user_access_token" \
  -H "Content-Type: application/json" \
  -d '{
    "product_id": "product_uuid",
    "product_name": "Product Name",
    "quantity": 2,
    "total_price": 199.98
  }'
```

**Результаты по ролям:**
- ✅ **Administrator**: Доступ разрешен
- ✅ **Manager**: Доступ разрешен
- ✅ **User**: Доступ разрешен

### 4. Пользователи (/api/business/users/)

#### Получение списка пользователей (только для админа)

```bash
curl -X GET http://localhost:8000/api/business/users/ \
  -H "Authorization: Bearer admin_access_token"
```

**Результаты по ролям:**
- ✅ **Administrator**: Доступ разрешен
- ❌ **Manager**: Доступ запрещен
- ❌ **User**: Доступ запрещен

## Матрица доступа

| Ресурс | Действие | Administrator | Manager | User |
|--------|----------|---------------|----------|------|
| **Магазины** | Чтение | ✅ (все) | ✅ (свой) | ✅ (все) |
| **Магазины** | Создание | ✅ | ✅ (свой) | ❌ |
| **Магазины** | Обновление | ✅ | ✅ (свой) | ❌ |
| **Магазины** | Удаление | ✅ | ✅ (свой) | ❌ |
| **Продукты** | Чтение | ✅ (все) | ✅ (свои) | ✅ (все) |
| **Продукты** | Создание | ✅ | ✅ (свои) | ❌ |
| **Продукты** | Обновление | ✅ | ✅ (свои) | ❌ |
| **Продукты** | Удаление | ✅ | ✅ (свои) | ❌ |
| **Заказы** | Чтение | ✅ (все) | ✅ (на свои товары) | ✅ (свои) |
| **Заказы** | Создание | ✅ | ✅ | ✅ |
| **Заказы** | Обновление | ✅ | ✅ (на свои товары) | ✅ (свои) |
| **Заказы** | Удаление | ✅ | ✅ (на свои товары) | ✅ (свои) |
| **Профили** | Чтение | ✅ | ✅ (свой) | ✅ (свой) |
| **Профили** | Обновление | ✅ | ✅ (свой) | ✅ (свой) |
| **Профили** | Удаление | ✅ | ✅ (свой) | ✅ (свой) |
| **Пользователи** | Чтение | ✅ | ❌ | ❌ |
| **Пользователи** | Управление | ✅ | ❌ | ❌ |

## Логика работы системы

### 🔄 **Пользовательский сценарий:**

1. **Регистрация/Вход** - Пользователь регистрируется или входит в систему
2. **Просмотр магазинов** - Пользователь видит список всех доступных магазинов
3. **Выбор магазина** - При клике на магазин показываются товары этого магазина
4. **Создание заказа** - Пользователь может заказать любой товар
5. **Управление заказами** - Пользователь видит и управляет только своими заказами
6. **Профиль** - Пользователь может редактировать свои данные и удалить аккаунт

### 🏪 **Менеджерский сценарий:**

1. **Вход** - Менеджер входит в систему
2. **Создание магазина** - Может создать свой магазин (если еще не имеет)
3. **Управление товарами** - Добавляет, редактирует, удаляет товары своего магазина
4. **Просмотр заказов** - Видит заказы на свои товары от пользователей
5. **Профиль** - Управляет своим профилем

### 🎯 **Администраторский сценарий:**

1. **Полный контроль** - Имеет доступ ко всем функциям системы
2. **Управление пользователями** - Видит и управляет всеми пользователями
3. **Мониторинг** - Контролирует все магазины, товары, заказы
4. **Поддержка** - Может решать любые проблемы в системе

## Тестовые аккаунты

```
Администратор: admin@example.com / admin123
Менеджер: manager@example.com / manager123
Пользователь: user@example.com / user123
```

## Коды ошибок

- **200 OK** - Успешное выполнение запроса
- **201 Created** - Ресурс успешно создан
- **400 Bad Request** - Неверные данные в запросе
- **401 Unauthorized** - Требуется аутентификация
- **403 Forbidden** - Недостаточно прав доступа
- **404 Not Found** - Ресурс не найден
- **500 Internal Server Error** - Внутренняя ошибка сервера

## Примеры использования

### Полный сценарий работы пользователя

```bash
# 1. Регистрация нового пользователя
curl -X POST http://localhost:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "customer@example.com",
    "full_name": "John Customer",
    "password": "password123",
    "password_confirm": "password123",
    "role": "user"
  }'

# 2. Вход в систему
TOKEN=$(curl -s -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"email": "customer@example.com", "password": "password123"}' | \
  python3 -c "import sys, json; print(json.load(sys.stdin)['access_token'])")

# 3. Просмотр магазинов
curl -X GET http://localhost:8000/api/business/shops/ \
  -H "Authorization: Bearer $TOKEN"

# 4. Просмотр товаров магазина
curl -X GET http://localhost:8000/api/business/shops/{shop_id}/products/ \
  -H "Authorization: Bearer $TOKEN"

# 5. Создание заказа
curl -X POST http://localhost:8000/api/business/orders/create/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "product_id": "product_uuid",
    "product_name": "Product Name",
    "quantity": 1,
    "total_price": 99.99
  }'

# 6. Просмотр своих заказов
curl -X GET http://localhost:8000/api/business/orders/ \
  -H "Authorization: Bearer $TOKEN"

# 7. Обновление профиля
curl -X PUT http://localhost:8000/api/business/profile/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"full_name": "John Updated"}'
```

## Фронтенд

Система включает современный фронтенд по адресу: `http://localhost:8000/`

### Функциональность фронтенда:

- 🎨 **Современный дизайн** с Bootstrap 5
- 📱 **Адаптивный интерфейс** для всех устройств
- 🔐 **Регистрация и вход** с выбором роли
- 🏪 **Просмотр магазинов** и их товаров
- 🛒 **Создание заказов** в один клик
- 📦 **Управление заказами** с отслеживанием статуса
- 👤 **Профиль пользователя** с возможностью редактирования
- ⚙️ **Панель менеджера** для управления магазином
- 🎛️ **Панель администратора** для управления системой

### Особенности:

- **JWT аутентификация** с автоматическим обновлением токенов
- **Правильное разделение прав** в зависимости от роли
- **Интуитивный интерфейс** для каждой роли
- **Real-time обновления** статусов заказов
- **Валидация форм** на стороне клиента и сервера

Эта документация предоставляет полное описание API для интеграции и использования маркетплейса.
