# 📚 API Документация

## 🌐 Общая информация

- **Базовый URL**: `http://localhost:8000/api`
- **Формат данных**: JSON
- **Аутентификация**: JWT Bearer Token
- **CORS**: Разрешены все origins

## 🔐 Аутентификация

### Регистрация пользователя

**POST** `/auth/register/`

Регистрация нового пользователя с автоматическим входом.

**Тело запроса:**
```json
{
  "email": "user@example.com",
  "first_name": "Иван",
  "last_name": "Иванов",
  "password": "password123",
  "password_confirm": "password123",
  "role": "user"
}
```

**Ответ:**
```json
{
  "message": "User registered successfully",
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "refresh_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "user": {
    "id": "uuid-string",
    "email": "user@example.com",
    "full_name": "Иванов Иван",
    "roles": ["user"]
  }
}
```

### Вход в систему

**POST** `/auth/login/`

Аутентификация пользователя и получение JWT токенов.

**Тело запроса:**
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

**Ответ:**
```json
{
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "refresh_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "user": {
    "id": "uuid-string",
    "email": "user@example.com",
    "full_name": "Иванов Иван",
    "roles": ["user"]
  }
}
```

### Ошибки входа

**Неверные данные:**
```json
{
  "error": "Неверный логин или пароль"
}
```

**Забаненный пользователь:**
```json
{
  "error": "Ваш аккаунт забанен до 12.02.2026 14:08"
}
```

### Выход из системы

**POST** `/auth/logout/`

Завершение сессии и инвалидация токенов.

**Заголовки:**
```
Authorization: Bearer {access_token}
```

**Ответ:**
```json
{
  "message": "Successfully logged out"
}
```

### Профиль пользователя

**GET** `/auth/profile/`

Получение информации о текущем пользователе.

**Заголовки:**
```
Authorization: Bearer {access_token}
```

**Ответ:**
```json
{
  "id": "uuid-string",
  "email": "user@example.com",
  "first_name": "Иван",
  "last_name": "Иванов",
  "patronymic": "Иванович",
  "full_name": "Иванов Иван Иванович",
  "roles": ["user"],
  "created_at": "2026-02-05T14:30:00Z",
  "updated_at": "2026-02-05T14:30:00Z"
}
```

## 🏪 Управление магазинами

### Список магазинов

**GET** `/business/shops/`

Получение списка всех магазинов.

**Заголовки:**
```
Authorization: Bearer {access_token}
```

**Ответ:**
```json
{
  "shops": [
    {
      "id": "uuid-string",
      "name": "Магазин №1",
      "address": "ул. Примерная, 123",
      "phone": "+7 (999) 123-45-67",
      "created_at": "2026-02-05T14:30:00Z",
      "updated_at": "2026-02-05T14:30:00Z"
    }
  ]
}
```

### Создание магазина

**POST** `/business/shops/`

Создание нового магазина (требуются права manager или admin).

**Заголовки:**
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "name": "Новый магазин",
  "address": "ул. Новая, 456",
  "phone": "+7 (999) 987-65-43"
}
```

**Ответ:**
```json
{
  "message": "Shop created successfully",
  "shop": {
    "id": "uuid-string",
    "name": "Новый магазин",
    "address": "ул. Новая, 456",
    "phone": "+7 (999) 987-65-43",
    "created_at": "2026-02-05T14:30:00Z",
    "updated_at": "2026-02-05T14:30:00Z"
  }
}
```

### Обновление магазина

**PUT** `/business/shops/{shop_id}/`

Обновление информации о магазине (только владелец или admin).

**Заголовки:**
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "name": "Обновленный магазин",
  "address": "ул. Обновленная, 789",
  "phone": "+7 (999) 555-55-55"
}
```

**Ответ:**
```json
{
  "message": "Shop updated successfully",
  "shop": {
    "id": "uuid-string",
    "name": "Обновленный магазин",
    "address": "ул. Обновленная, 789",
    "phone": "+7 (999) 555-55-55",
    "created_at": "2026-02-05T14:30:00Z",
    "updated_at": "2026-02-05T15:30:00Z"
  }
}
```

### Удаление магазина

**DELETE** `/business/shops/{shop_id}/`

Удаление магазина (только владелец или admin).

**Заголовки:**
```
Authorization: Bearer {access_token}
```

**Ответ:**
```json
{
  "message": "Shop deleted successfully"
}
```

## 🛍️ Управление товарами

### Список товаров магазина

**GET** `/business/shops/{shop_id}/products/`

Получение списка товаров конкретного магазина.

**Заголовки:**
```
Authorization: Bearer {access_token}
```

**Ответ:**
```json
{
  "products": [
    {
      "id": "uuid-string",
      "name": "Товар №1",
      "description": "Описание товара",
      "price": 999.99,
      "shop": {
        "id": "shop-uuid",
        "name": "Магазин №1"
      },
      "created_at": "2026-02-05T14:30:00Z",
      "updated_at": "2026-02-05T14:30:00Z"
    }
  ]
}
```

### Создание товара

**POST** `/business/shops/{shop_id}/products/`

Создание нового товара в магазине (требуются права manager или admin).

**Заголовки:**
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "name": "Новый товар",
  "description": "Описание нового товара",
  "price": 1499.99
}
```

**Ответ:**
```json
{
  "message": "Product created successfully",
  "product": {
    "id": "uuid-string",
    "name": "Новый товар",
    "description": "Описание нового товара",
    "price": 1499.99,
    "shop": {
      "id": "shop-uuid",
      "name": "Магазин №1"
    },
    "created_at": "2026-02-05T14:30:00Z",
    "updated_at": "2026-02-05T14:30:00Z"
  }
}
```

## 📦 Управление заказами

### Создание заказа

**POST** `/business/orders/`

Создание нового заказа.

**Заголовки:**
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "product": "product-uuid",
  "quantity": 2
}
```

**Ответ:**
```json
{
  "message": "Order created successfully",
  "order": {
    "id": "uuid-string",
    "product": {
      "id": "product-uuid",
      "name": "Товар №1",
      "price": 999.99
    },
    "quantity": 2,
    "total_price": 1999.98,
    "status": "pending",
    "created_at": "2026-02-05T14:30:00Z"
  }
}
```

### Список заказов

**GET** `/business/orders/`

Получение списка заказов (своих для пользователей, всех для manager/admin).

**Заголовки:**
```
Authorization: Bearer {access_token}
```

**Ответ:**
```json
{
  "orders": [
    {
      "id": "uuid-string",
      "product": {
        "id": "product-uuid",
        "name": "Товар №1",
        "price": 999.99
      },
      "quantity": 2,
      "total_price": 1999.98,
      "status": "pending",
      "created_at": "2026-02-05T14:30:00Z"
    }
  ]
}
```

## 👥 Управление пользователями (только для админа)

### Список пользователей

**GET** `/business/users/`

Получение списка всех пользователей с информацией о банах.

**Заголовки:**
```
Authorization: Bearer {access_token}
```

**Ответ:**
```json
{
  "users": [
    {
      "id": "uuid-string",
      "email": "user@example.com",
      "full_name": "Иванов Иван",
      "roles": ["user"],
      "is_active": true,
      "ban_until": null,
      "created_at": "2026-02-05T14:30:00Z"
    },
    {
      "id": "uuid-string-2",
      "email": "banned@example.com",
      "full_name": "Петров Петр",
      "roles": ["user"],
      "is_active": false,
      "ban_until": "2026-02-12T14:08:00Z",
      "created_at": "2026-02-05T14:30:00Z"
    }
  ]
}
```

### Обновление пользователя

**PUT** `/business/users/{user_id}/update/`

Обновление данных пользователя, включая бан/разбан.

**Заголовки:**
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "full_name": "Петров Петр Сергеевич",
  "is_active": false,
  "ban_until": "2026-02-15T14:08:00Z"
}
```

**Ответ:**
```json
{
  "message": "User updated successfully",
  "user": {
    "id": "uuid-string-2",
    "email": "banned@example.com",
    "full_name": "Петров Петр Сергеевич",
    "roles": ["user"],
    "is_active": false,
    "ban_until": "2026-02-15T14:08:00Z"
  }
}
```

### Удаление пользователя

**DELETE** `/business/users/{user_id}/delete/`

Удаление пользователя (нельзя удалить суперпользователя или самого себя).

**Заголовки:**
```
Authorization: Bearer {access_token}
```

**Ответ:**
```json
{
  "message": "User deleted successfully"
}
```

## 🔒 Права доступа

### Роли и их права

#### User (Пользователь)
- Просмотр магазинов и товаров
- Создание и просмотр своих заказов
- Управление своим профилем

#### Manager (Менеджер)
- Все права пользователя
- Создание/редактирование/удаление магазинов и товаров
- Просмотр всех заказов

#### Admin (Администратор)
- Все права менеджера
- Полное управление пользователями
- Блокировка/разблокировка аккаунтов
- Удаление пользователей (кроме суперпользователей)

### Ограничения

- Нельзя удалить суперпользователя
- Нельзя удалить самого себя
- Нельзя изменять/удалять чужие магазины (кроме admin)
- Заблокированные пользователи не могут войти в систему

## 🚨 Коды ошибок

### HTTP статусы

- `200` - Успешное выполнение
- `201` - Ресурс создан
- `400` - Неверные данные запроса
- `401` - Требуется аутентификация
- `403` - Недостаточно прав доступа
- `404` - Ресурс не найден
- `500` - Внутренняя ошибка сервера

### Формат ошибок

```json
{
  "error": "Текст ошибки"
}
```

## 📝 Примеры использования

### Полный цикл работы с API

```bash
# 1. Регистрация
curl -X POST http://localhost:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","first_name":"Иван","last_name":"Иванов","password":"password123","password_confirm":"password123","role":"user"}'

# 2. Вход
TOKEN=$(curl -s -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"password123"}' | \
  python3 -c "import sys, json; print(json.load(sys.stdin)['access_token'])")

# 3. Получение списка магазинов
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:8000/api/business/shops/

# 4. Создание заказа
curl -X POST http://localhost:8000/api/business/orders/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"product":"product-uuid","quantity":1}'
```

---

**Документация актуальна для версии API 1.0**
